export const metadata = {
  title: "UNO-BiDirectional Translation Next App",
  description: "Translate with more than just words",
};

export default function RootLayout({ children }) {
 return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
